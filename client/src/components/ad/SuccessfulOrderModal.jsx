import Button from "react-bootstrap/Button";
import Modal from "react-bootstrap/Modal";

export default function SuccessfulOrderModal({ hideSuccessfulOrderModal }) {
  return (
   <p>thank u for your order</p>
  );
}